services:
  - type: web
    name: epic-offers-bridge
    runtime: python
    rootDir: services/epic-offers-bridge
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: BRIDGE_SECRET
        sync: false
